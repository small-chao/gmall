spring:
  application:
    name: pms-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848 # Nacos作为配置中心
        file-extension: yml # 这里指定的文件格式需要和nacos上新建的配置文件后缀相同，否则读不到
        namespace: abd9e97c-06b4-4152-a14b-12b7e37b10ba
        group: dev
