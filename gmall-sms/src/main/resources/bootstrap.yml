spring:
  application:
    name: sms-service
  cloud:
    nacos:
      discovery:
        config:
          server-addr: 127.0.0.1:8848 # Nacos作为配置中心
          file-extension: yml # 这里指定的文件格式需要和nacos上新建的配置文件后缀相同，否则读不到
          namespace: e76eccd8-a464-446d-8d1a-07b2e9ea8a3b
          group: dev
